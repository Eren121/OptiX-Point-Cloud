#ifndef TRIANGLE_H
#define TRIANGLE_H

#include <cuda_runtime.h>

// Classe utilitaire pour pour simplifier l'utilisation d'un Triangle sur GPU
struct d_Triangle
{
    // Construit un triangle de 3 sommets a, b, c
    d_Triangle(float3 a, float3 b, float3 c)
    {

    }
};

#endif /* TRIANGLE_H */
